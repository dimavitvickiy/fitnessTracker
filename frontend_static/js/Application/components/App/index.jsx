import React from 'react';

import ScheduleInputChange from '../../containers/scheduleInputChange'

import {Main} from '../Main/index';

export const App = () => (
    <div>
        <ScheduleInputChange/>
        <Main/>
    </div>
);